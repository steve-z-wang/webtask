# LLM API Keys
# OPENAI_API_KEY=sk-...
# GOOGLE_API_KEY=...

# Logging level (DEBUG, INFO, WARNING, ERROR)
# WEBTASK_LOG_LEVEL=INFO

# Debug mode (saves screenshots/context/summary to disk)
# WEBTASK_DEBUG=1
# WEBTASK_DEBUG_DIR=debug
# Output: summary.txt, *_context.txt, *_img_*.png

# Test recording/replay mode
# WEBTASK_TEST_MODE=record   # Record LLM and browser interactions to fixtures
# WEBTASK_TEST_MODE=replay   # Replay from recorded fixtures (offline, deterministic)
# (unset for live mode - normal operation without recording)
